# Compatibility pointer for legacy backend-local workflows.
# Copy ../.env.example to the repository root as ../.env.
# Root .env is the canonical backend configuration; this file is intentionally
# not a duplicate template. A real backend/.env remains supported as a legacy
# fallback, and BACKEND_ENV_FILE remains an explicit test/launcher override.
