# Compatibility pointer for the legacy Product workspace dotenv workflow.
# Copy ../../.env.example to the repository root as ../../.env.
# Root .env is canonical. A real apps/product/.env.local or .env remains a
# legacy fallback, and process/CLI FRONTEND_PORT overrides remain supported.
