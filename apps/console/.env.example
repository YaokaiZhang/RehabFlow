# Compatibility pointer for the legacy Console workspace dotenv workflow.
# Copy ../../.env.example to the repository root as ../../.env.
# Root .env is canonical. A real apps/console/.env.local or .env remains a
# legacy fallback, and process/CLI FRONTEND_PORT overrides remain supported.
